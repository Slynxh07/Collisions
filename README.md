Elastic Collision simulator made in C
Made with SDL2
Built with CMake
CMake will fetch SDL2 if not found locally

To build:
```bash
cd Collisions
mkdir build
cd build
cmake ..
cmake --build .
./Collisions
```